trigger:
- master

pool:
  # vmImage: 'ubuntu-latest'
  name: 'azure-agent-vmss'

jobs:
- job: job1
  steps:
  - script: echo hello, $HOSTNAME

- job: job2
  steps:
  - script: echo hello, $HOSTNAME

- job: job3
  steps:
  - script: echo hello, $HOSTNAME

- job: job4
  steps:
  - script: echo hello, $HOSTNAME